<aside >

      <h2 class="text-2xl text-center p-3 text-red-600 text-3xl">Find the perfect gift for this holiday season!</h2>
<div class="text-mid grid grid-cols-4 gap-4 m-8 px-8 bg-red-200 content-between">

</aside>
      {{#each products}}
      <div class= "rounded-2xl border-solid border-2 border-blue-500 bg-green-200 p-5 pl-8">
        <h3 class="items-center">
        {{product_name}}
        </h3>
        <img src="{{image_url}}" class="object-cover w-32 h-32 mx-07">
        <h3 class="mx-07">
        Price: ${{price}}
        </h3>
        <h3>
        Quantity: {{stock}}
        </h3>
      </div>
      {{/each}}
      </div>
